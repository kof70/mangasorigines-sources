<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MangasOriginesSource">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Kotatsu and its forks (Kotatsu-Redo, Futon, Usagi) discover external sources by
            querying the package manager for content providers answering one of these actions.
            The provider label is what shows up as the source name inside the reader.
        -->
        <provider
            android:name=".MangasOriginesContentProvider"
            android:authorities="${applicationId}.provider"
            android:exported="true"
            android:label="@string/source_name">
            <intent-filter>
                <!-- Kotatsu, Kotatsu-Redo, Usagi -->
                <action android:name="app.kotatsu.parser.PROVIDE_MANGA" />
                <!-- Futon -->
                <action android:name="app.futon.parser.PROVIDE_MANGA" />
            </intent-filter>
        </provider>
    </application>

</manifest>
